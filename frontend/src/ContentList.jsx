import { useEffect, useState } from 'react';
import styles from './ContentList.module.css';

export default function ContentList({ token, role }) {
  // Optionally, you can remove all logic and just return null or a message.
  return null;
} 